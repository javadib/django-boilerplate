{% if use_drf -%}
import pytest
from rest_framework.test import APIClient


@pytest.fixture
def api_client():
    return APIClient()
{% else -%}
# Add shared pytest fixtures here as your test suite grows.
{%- endif %}
